TOODs: 
1. Definitions of service types and how to "stop" each of them;
2. Cleanup work after stopping service (final log update, etc.);
3. Mark instances as "stopping" and let the background task stop them "for real" (by calling boto3 api);

